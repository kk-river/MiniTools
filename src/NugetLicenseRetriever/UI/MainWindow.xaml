<Window
    x:Class="NugetLicenseRetriever.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="NuGet License Retriever" 
    Height="800" 
    Width="1200"
    WindowStartupLocation="CenterScreen"
    ResizeMode="CanResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="NuGet License Retriever" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <!-- File Selection -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox x:Name="FilePathTextBox"
                     Grid.Column="0"
                     Margin="0,0,10,0"
                     Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Select a .sln or .csproj file..."/>
            
            <Button x:Name="BrowseButton"
                    Grid.Column="1"
                    Click="BrowseButton_Click"
                    MinWidth="100">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" 
                               Text="&#xE8B7;" 
                               Margin="0,0,5,0"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="Browse" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    Margin="0,0,0,20">
            <Button x:Name="AnalyzeButton"
                    Click="AnalyzeButton_Click"
                    Margin="0,0,10,0"
                    MinWidth="150">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" 
                               Text="&#xE721;" 
                               Margin="0,0,5,0"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="Analyze Packages" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            
            <Button x:Name="ExportButton"
                    Click="ExportButton_Click"
                    IsEnabled="False"
                    MinWidth="130">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" 
                               Text="&#xE74E;" 
                               Margin="0,0,5,0"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="Export to File" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Results -->
        <TabControl Grid.Row="3" x:Name="ResultsTabControl">
            <TabItem Header="All Packages">
                <DataGrid x:Name="AllPackagesDataGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          CanUserReorderColumns="True"
                          CanUserSortColumns="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Project Path" Binding="{Binding Path}" Width="200"/>
                        <DataGridTextColumn Header="Package ID" Binding="{Binding LicenseInfo.Id}" Width="200"/>
                        <DataGridTextColumn Header="Version" Binding="{Binding LicenseInfo.Version}" Width="100"/>
                        <DataGridTextColumn Header="License" Binding="{Binding LicenseInfo.License}" Width="*"/>
                        <DataGridCheckBoxColumn Header="Is Transitive" Binding="{Binding LicenseInfo.IsTransitivePackage}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            
            <TabItem Header="Unique Packages">
                <DataGrid x:Name="UniquePackagesDataGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          CanUserReorderColumns="True"
                          CanUserSortColumns="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Package ID" Binding="{Binding Id}" Width="200"/>
                        <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="100"/>
                        <DataGridTextColumn Header="License" Binding="{Binding License}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <Grid Grid.Row="4" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="StatusTextBlock" 
                       Grid.Column="0"
                       Text="Ready"
                       VerticalAlignment="Center"
                       Foreground="#605E5C"/>
            
            <ProgressBar x:Name="AnalysisProgressBar"
                         Grid.Column="1"
                         Width="200"
                         Height="6"
                         IsIndeterminate="True"
                         Visibility="Collapsed"
                         Background="#E1DFDD"
                         Foreground="#0078D4"/>
        </Grid>
    </Grid>
</Window>
